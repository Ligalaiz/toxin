mixin date-picker-card(option = {})
  -
    const { id = '', pickerType = '', amount = '' } = option;

    const cls = pickerType === 'double' && amount === '2' ? 'second' : '';

  .date-picker__wrap(id=`pickerWrap-${id}-${pickerType}` class=`${cls}`)
